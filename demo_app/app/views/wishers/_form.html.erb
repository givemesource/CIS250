<%= form_for(@wisher) do |f| %>
  <% if @wisher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wisher.errors.count, "error") %> prohibited this wisher from being saved:</h2>

      <ul>
      <% @wisher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
